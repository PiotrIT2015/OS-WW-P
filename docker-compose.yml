services:
  # 1. Usługa aplikacji Python
  app:
    build: .
    container_name: os-ww-p
    restart: unless-stopped
    volumes:
      - .:/app
    ports:
      - "55000:80"
    environment:
      - MYSQL_HOST=db
      - MYSQL_USER=root
      - MYSQL_PASSWORD=" "
      - MYSQL_DATABASE=search_db
    depends_on:
      - db

  # 2. Usługa serwera Apache
  apache:
    image: httpd:2.4
    container_name: apache_server
    restart: unless-stopped
    ports:
      - "55001:80"
    volumes:
      - ./apache.conf:/usr/local/apache2/conf/extra/httpd-vhosts.conf
    depends_on:
      - app

  # 3. Usługa bazy danych MySQL
  db:
    image: mysql:8.0
    container_name: mysql_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: mydatabase
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    volumes:
      - mysql_data:/var/lib/mysql

  # 4. Usługa do zarządzania bazą danych przez przeglądarkę
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: phpmyadmin_ui
    restart: unless-stopped
    ports:
      - "55002:80"
    environment:
      - PMA_HOST=db
      - PMA_PORT=3306
    depends_on:
      - db

# Definicja nazwanego woluminu
volumes:
  mysql_data: